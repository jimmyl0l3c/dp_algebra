import 'package:flutter/material.dart';

class CalcMatrices extends StatelessWidget {
  const CalcMatrices({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Kalkulačka - Operace s maticemi'),
      ),
      body: Container(),
    );
  }
}
